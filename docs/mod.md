# Exporter mod

Implemented in `../mod/`: manual per-map export plus an opt-in automatic
whole-catalog mode (two-step confirm, Easy Sandbox loader, fault stops the run).
Manual loading is the default workflow; the runner is for full refreshes.
